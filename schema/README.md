# Workflow schemas

TaskNotes Workflows writes the canonical mdbase runtime `workflow/0.1` record
defined by `@callumalpass/mdbase-runtime`. The package validates every generated
record with that package before writing it.

`tasknotes-workflow.schema.json` documents the compatibility input accepted for
files created by TaskNotes Workflows 0.1.x. It is not an authoring target.
